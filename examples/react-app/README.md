# React Example

Demonstrates using the `<multi-editor>` web component in a React application via the `@lit/react` wrapper.

## Setup

```bash
npm install
npm run dev
```

Opens on http://localhost:3001. Requires the relay server running on port 8080.
